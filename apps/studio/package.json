{
  "name": "studio",
  "version": "0.0.0",
  "license": "BUSL-1.1",
  "scripts": {
    "dev": "prisma studio --schema ../../packages/database/prisma/schema.prisma --port 3005",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "echo 'No TypeScript files to check in Prisma Studio wrapper' && exit 0"
  },
  "devDependencies": {
    "prisma": "6.4.1"
  }
}
