# @repo/jsonapi-adapter

Utility module for parsing and flattening JSON:API-compliant server responses in the Zopio framework.

## Features

- Normalize `data` and `included` resources
- Handle relationships as embedded objects
- Flatten nested JSON:API entities to usable structures