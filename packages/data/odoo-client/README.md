# @repo/odoo-client

Odoo JSON-RPC client adapter compatible with @repo/crud interface.

Implements Odoo's `search_read`, `read`, `create`, `write`, and `unlink` methods.

> Note: You must provide db, username, password, and url to establish session.