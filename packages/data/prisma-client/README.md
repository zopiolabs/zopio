# @repo/prisma-client

Prisma ORM adapter for the @repo/crud interface. Designed to be used within backend services in the Zopio framework.

## Features

- Compatible with all @repo/crud consumers
- Abstracted Prisma access layer
- Can be extended with filters, pagination, and relations

> Note: This is a backend-only module and requires access to Prisma Client.