# @repo/data-table (Layout Builder)

Zopio useTable with support for column order, visibility and width saving.

## Features

- Drag & drop column reordering
- Column width persistence
- Visibility toggle with layout save

## Usage

```tsx
toggleColumn("email");
reorderColumns(["id", "name", "email"]);
```
