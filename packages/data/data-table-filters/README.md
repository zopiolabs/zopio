# @repo/data-table (Advanced Filters)

`useTable` hook for Zopio with smart filter builder support.

## Features

- Multi-field filters
- Operator-based conditions (`equals`, `contains`, `gte`, `lte`)
- Typed filter state

## Usage

```tsx
addFilter({ field: "price", operator: "gte", value: 100 });
```
