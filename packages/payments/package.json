{
  "name": "@repo/payments",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "dependencies": {
    "@stripe/agent-toolkit": "^0.7.12",
    "@t3-oss/env-nextjs": "^0.13.10",
    "server-only": "^0.0.1",
    "stripe": "^20.0.0",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "typescript": "^5.9.3"
  }
}
